import { View } from "react-native";
import Home from "shared/components/pages/Home";

export default function _screen() {
  return (
    <View style={{ backgroundColor: '#222324', flex: 1}}>
      <Home/>
    </View>
  );
}